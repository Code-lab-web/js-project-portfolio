padding: ${props.theme.spacing.medium};
import { 
    ThemeProvider } from "styled-components"
const App = () =>
    return{
        <ThemeProvider theme(theme>
            <GlobalStyle />
            <Header />
            </ThemeProvider>
        )
    }
export const theme = {
    spacing: {
        small: '0.5rem',
        medium: '1rem',
        large: '2rem'
    },
    colours:{
        
    }
    }
}